<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet" href="css/style.css">
</head>
<body>
    <h1>Todos</h1>
    <ul>
    <% todos.forEach( el => { %>
            <li class='todoItem' data-id='<%=el._id%>'>
                <span class='<%= el.completed === true ? 'completed' : 'not'%>'><%= el.segmentName %></span>
                <span class='<%= el.completed === true ? 'completed' : 'not'%>'><%= el.segmentId %></span>
                <span class='<%= el.completed === true ? 'completed' : 'not'%>'><%= el.percentageOff %></span>
                <span class='<%= el.completed === true ? 'completed' : 'not'%>'><%= el.timeOffXom %></span>
                <span class='<%= el.completed === true ? 'completed' : 'not'%>'><%= el.percentOffLB%></span>
                <span class='<%= el.completed === true ? 'completed' : 'not'%>'><%= el.timeOffLB%></span>
                <span class='<%= el.completed === true ? 'completed' : 'not'%>'><%= el.rank %></span>
                <span class='del'> Delete </span>
            </li>
    <% }) %>    
    </ul>

    <h2><%= user.userName %> has <%= left %> things left to do.</h2>

    <form action="/todos/createTodo" method='POST'>
        <input type="text" placeholder="Enter Todo Item" name='todoItem'>
        <input type="submit">
    </form>

    <a href="/logout">Logout</a>
    <a href="todos/getlink">Link your Strava Account!</a>
    <a href="todos/getActivities">Get activities!</a>
    <a href="todos/getSegments">Get effort list!</a>
    

    <script src="js/main.js"></script>
</body>
</html>